packages:
  all:
    compiler:
      - clang
      - gcc
    providers:
      mpi:
        - mpich
      blas:
        - netlib-lapack
      lapack:
        - netlib-lapack
    variants: build_type=Release ~shared
  cmake:
    version: [brew-latest]
    buildable: false
    paths:
      cmake@brew-latest: /usr/local
